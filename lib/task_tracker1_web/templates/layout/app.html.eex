<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>TaskTracker1 · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
  </head>
  <body>
    <div class = "container-fluid">
      <nav class="navbar navbar-header">
        <div class="navbar-brand" href="#">Task Tracker</div>
        <div class="nav navbar-nav">
          <%= if @current_user do %>
            <p class="my-3">
              <a class="navbar-brand"> Hi <%= @current_user.name%>!</a>
              <%= link("Profile", to: Routes.user_path(@conn, :index), class: "navbar-toggler") %>
              <%= link("Tasks", to: Routes.task_path(@conn, :index), class: "navbar-toggler") %> 
              <%= link("Logout", to: Routes.session_path(@conn, :delete),
                method: :delete, class: "navbar-toggler") %>
            </p>
          <% end %>
        </div>
      </nav>

      <div class="row">
        <div class="col-12">
          <%= if get_flash(@conn, :info) do %>
            <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
          <% end %>
          <%= if get_flash(@conn, :error) do %>
            <p class="alert alert-danger" role="alert">
              <%= get_flash(@conn, :error) %>
            </p>
          <% end %>
        </div>
      </div>
      <%= render @view_module, @view_template, assigns %>
    </div>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
    <script>window.csrfToken = "<%= Plug.CSRFProtection.get_csrf_token() %>"</script>
  </body>
</html>
